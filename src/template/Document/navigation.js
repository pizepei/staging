layui.define( [ 'form','laytpl'],function(exports) {
    $ = layui.$
        , layer = layui.layer
        , form = layui.form
        , laytpl = layui.laytpl

    var navigation = {
        data:{

        },
        /**
         * 初始化
         * @param comfig
         */
        render:function (comfig={}) {
            // 请求后端获取对应的数据规格
            // 请求后端获取菜单数据
            //  请求后端获取后端版本

            //实例化渲染导航菜单
        }
    }
    exports('navigation', navigation)
})